<% provide :title, t("edit_word") %>
<% provide :button_submit, t("save_changes")%>
<h1><%= t "edit_word" %> <%= @word.content %></h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for [:admin, :category, @word] do |f| %>
      <%= render "form", f: f %>
    <% end %>
  </div>
</div>
